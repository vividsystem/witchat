[project]
name = "ragdoll"
version = "0.1.0"
description = "An encrypted p2p chat app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncio>=4.0.0",
    "kademlia>=2.2.3",
    "pynacl>=1.6.0",
    "textual>=6.3.0",
]
